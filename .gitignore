# --- Raw/intermediate data not needed by the app — keep LOCAL only ---
# (re-fetchable via data_prep/fetch_srtm_slope.py; only the small resampled
# penang_{state,island}_slope.tif outputs are actually used/committed)
data/population/mys_ppp_2020_UNadj.tif
data/terrain/N05E100.hgt
data/terrain/N05E100.hgt.gz

# The app only needs these committed (all < 5 MB):
#   data/towers_penang/502.csv
#   data/population/penang_ppp_2020.tif
#   data/villages/penang_places.geojson

# --- Python ---
__pycache__/
*.py[cod]
.venv/
.venv-ml/
venv/
env/

# --- Streamlit / build artifacts ---
outputs/
*.html
.streamlit/secrets.toml

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# Deployment secrets
.env

# SiteSense 5G — Streamlit Community Cloud dependencies
# Tested locally with the versions noted in comments (Python 3.14).
# Floors keep the Cloud resolver flexible; the app also falls back from
# leafmap to plain folium if leafmap fails to build (see app.py).

streamlit>=1.37,<2        # tested 1.60.0
streamlit-folium>=0.21    # tested 0.27.4
leafmap>=0.30             # tested 0.63.1  (map layer)
folium>=0.16              # tested 0.20.0  (fallback map backend)
pandas>=2.1               # tested 3.0.5
numpy>=1.26               # tested 2.5.1
rasterio>=1.3             # tested 1.5.0   (WorldPop raster)
shapely>=2.0             # tested 2.1.2
requests>=2.31            # calls the FastAPI /predict-rsrp demo panel over HTTP

# --- PostGIS migration (Physical Finale, db.py / data_prep/load_postgis.py) -
# Not imported by app.py yet, so this does not affect the deployed
# Streamlit Cloud app. Needed only to run the loader / future FastAPI layer.
psycopg2-binary>=2.9

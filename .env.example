# Copy to .env on the server and set real credentials:  cp .env.example .env
# .env is gitignored — never commit real credentials.
DB_PASSWORD=change_me_to_a_strong_password

# Dashboard login (in-app gate, see app.py's require_login()).
APP_USERNAME=admin
APP_PASSWORD=change_me_to_a_strong_password

# SiteSense 5G — FastAPI + LSTM service dependencies.
# Deliberately SEPARATE from the root requirements.txt (Streamlit Cloud):
# TensorFlow/rasterio/psycopg2 are heavy and not needed by the dashboard's
# own deploy target. This is what runs in SiteSense5G_App/.venv-ml locally,
# and what a future DigitalOcean App Platform "API" component would install.
#
# Local setup:
#   py -3.12 -m venv .venv-ml          (TensorFlow has no Python 3.14 wheels yet)
#   .venv-ml/Scripts/python.exe -m pip install -r api/requirements.txt
# Run:
#   .venv-ml/Scripts/python.exe -m uvicorn api.main:app --reload --port 8001

fastapi>=0.115        # tested 0.141.1
uvicorn[standard]>=0.30  # tested 0.52.4
pandas>=2.1            # tested 3.0.5
numpy>=1.26             # tested 2.5.2

# Function 1/2/3 (towers / coverage-gap / recommend-sites)
rasterio>=1.3           # tested 1.5.1
shapely>=2.0             # tested 2.1.2
psycopg2-binary>=2.9      # PostGIS access via db.py

# LSTM RSRP prediction (ml/train_lstm_rsrp.py, api/main.py's /predict-rsrp)
tensorflow>=2.20          # tested 2.21.0 — no Python 3.14 wheel as of this writing, use 3.12
scikit-learn>=1.4          # tested 1.9.0
matplotlib>=3.8             # tested 3.11.1 — training-history plot only

# ml/build_training_notebook.py only (writes + executes the .ipynb deliverable)
nbformat>=5.9
nbclient>=0.10
ipykernel>=6.29

# data_prep/fetch_ookla_penang.py only (reads Ookla's quarterly parquet tiles)
pyarrow>=17.0
